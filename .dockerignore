# Git
.git
.gitignore

# Environment Variables (CRITICAL: Never copy secrets into the image)
.env
.env.*

# Node Modules (Docker should install these fresh inside the container)
node_modules
frontend/node_modules
backend/node_modules

# Build outputs (Docker will build these inside)
frontend/dist
backend/dist

# Docker files
docker-compose.yml
dockerfiles/

# Logs and debug files
npm-debug.log
yarn-debug.log
yarn-error.log
*.log

# OS generated files
.DS_Store
Thumbs.db