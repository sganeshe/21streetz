const express = require('express');


const router = express.Router();


router.post("/new",);
router.get("/myorders",);
router.get("/:id",);
router.put("/:id/pay",);


router.get("/all",);
router.put("/:id/deliver",);

module.exports = router;
