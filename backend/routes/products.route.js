const express = require('express');


const router = express.Router();


router.get("/all",);
router.get("/:id",);


router.post("/add",);
router.post("/:id",);
router.put("/:id",);
router.delete("/:id",);

module.exports = router;
